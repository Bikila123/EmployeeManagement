package com.Internship.Backend.mappers;

//import com.Internship.Backend.models.AuditTrailModel;
//import com.Internship.Backend.models.CommissionFilterModel;
//import org.apache.ibatis.annotations.Select;
//
//import java.util.List;

public interface AuditTrailMapper {

//    @Select("SELECT ta.*,tu.username FROM tbl_audit_trail ta\n" +
//            "LEFT JOIN tbl_user tu ON ta.user_id = tu.id ORDER BY ta.id DESC") ORDER BY ta.id DESC

//    @Select("<script>" +
//            "SELECT ta.*,tu.username FROM tbl_audit_trail ta\n" +
//            "LEFT JOIN tbl_user tu ON ta.user_id = tu.id  WHERE 1=1 " +
//            "<if test='start_date != null and end_date != null'>\n" +
//            "AND ta.timestamp BETWEEN #{start_date} AND #{end_date}\n" +
//            "</if>" +
//            " ORDER BY ta.id DESC"+
//            "</script>")
//    List<AuditTrailModel> fetchAuditTrails(CommissionFilterModel model);
}
